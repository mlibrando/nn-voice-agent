"""Tool layer — mock-backend HTTP client + Realtime tool schemas.

Day 3 part 2 wires the real tool handlers here (customer_lookup, mutations,
etc.). Right now this package exposes only the shared async HTTP client via
`client.py`; `definitions.py` is a placeholder for the tool schemas.
"""
