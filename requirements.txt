fastapi>=0.115.0
uvicorn[standard]>=0.30.0
websockets>=13.0
python-dotenv>=1.0.0
certifi>=2024.0.0
httpx>=0.27.0
# Day 4: Twilio POSTs `From` as form-encoded data to /incoming-call, and
# Starlette's `await request.form()` needs this to parse it. Without it,
# the route raises AssertionError on every inbound call.
python-multipart>=0.0.9
